1 npm init -y
2 npm i -D parcel-bundler 
3 index.html
4 npm i -D jest
5 package.json 
"test" : "jest" 설정 
6 코드 작성
7 npm test 

-test('테스트설명',()=>{
    expect('검증대상').to로시작하는 matcher함수('기대결과')
})
matcher함수: toBe() = 숫자,문자 같은 객체가 아닌 기본형 테스트 ,
toEqual() = 객체 테스트, 
toContain() =배열 안에 항목 검증,